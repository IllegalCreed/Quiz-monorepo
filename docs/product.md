# Quiz Monorepo - 产品文档

## 产品定位

Quiz App 是一个面向开发者的知识问答平台，帮助用户通过答题巩固 JavaScript、数据库、网络协议、算法等技术知识。

**两端架构**：

| 端     | 应用                       | 目标用户               |
| ------ | -------------------------- | ---------------------- |
| 用户端 | `quiz-app`（端口 10000）   | 答题用户               |
| 管理端 | `quiz-admin`（端口 10050） | 内容管理员、超级管理员 |

---

## 用户端功能（quiz-app）

### 已实现

**答题核心流程**：

1. 页面加载自动获取一道随机题目
2. 展示题干 + 所有选项（答题前不显示解析）
3. 用户点击选项即提交，不可更改
4. 后端返回判定结果 + 解析
5. 答对：绿色高亮 + 展示所有选项解析 + 1 秒后自动跳转
6. 答错：红色高亮错误选项 + 绿色高亮正确选项 + 展示解析 + 手动点击下一题

**UI 特性**：

- 暗色/亮色主题切换（系统主题检测 + 手动切换）
- 响应式布局（移动端适配）
- 渐变标题 + 泛光效果

**用户身份（游客模式 + 可选绑定）**：

- 游客直接答题，无需登录，答题功能完整（后端 OptionalUserJwtGuard 已支持）
- Header 右侧显示「登录」按钮，点击弹出**对话框**（非独立页面，不中断答题流程）
- 对话框内包含登录/注册两个 Tab，使用共享 UI 库的 BaseDialog + BaseInput 组件
- 登录后将游客历史同步到服务器（后端 AnswerAttempt 已支持 userId 关联）

**用户菜单（登录后）**：

- Header 右侧「登录」按钮替换为用户头像/图标 + 用户名
- 点击展开下拉菜单：做题历史 / 分类偏好 / 退出登录

**做题历史（侧边抽屉面板）**：

- 从右侧滑出，不离开答题页面
- 筛选：全部 / 答对 / 答错
- 卡片列表：题干(截断) + 正误徽标 + 选择的选项 + 相对时间
- 无限滚动加载（触底自动加载下一页）
- 未登录时显示提示「登录后查看答题记录」

**偏好分类（Miller Columns 分栏选择器）**：

- 答题页显示「选择分类」按钮 + 已选分类标签
- 点击弹出对话框，采用 macOS Finder 风格的分栏浏览器：
  - 左列维度 → 右列逐级展开分类，点击文字展开、点击指示器选中
  - 三态选中：全选 / 半选（部分子级选中）/ 未选中
  - 选中父级自动包含所有子级
- 顶部搜索框跨层级过滤 + 已选摘要可逐个移除
- 已登录 → 偏好同步到服务器；游客 → 保存在 localStorage
- **叶子节点约束**：题目只能挂到叶子分类；每个非叶子节点自带「通识」子节点，收纳不适合细分的题目。通识节点显示名拼接父名（如「前端通识」），创建/删除子分类时自动维护（含题目关联和用户偏好的迁移）

**登录引导（游客答错弹窗）**：

- 游客答错时弹出 BaseDialog 登录引导弹窗，答对不弹出（自动跳下一题）
- 用户可选「不再提示」永久关闭（localStorage 持久化）
- 点击「去登录」打开登录对话框

**实时通知（SSE）**：

- 客户端启动自动建立 SSE 连接（EventSource 内置自动重连）
- 管理员广播刷新事件时，客户端自动 reload 页面
- 公告推送：顶部 toast 提示，5 秒自动消失
- 心跳上报：路由变更、登录/退出时通过 HTTP POST 更新客户端状态

**流量统计**：

- Google Analytics (GA4)：国际流量统计（Measurement ID: `G-F61SMFC46V`）
- 后续计划接入百度统计覆盖国内流量

---

## 管理端功能（quiz-admin）

### 已实现

| 模块       | 功能                                                                           |
| ---------- | ------------------------------------------------------------------------------ |
| 认证       | JWT 登录、7 天有效期、刷新不丢失、**生产环境隐藏测试账号**                     |
| 权限系统   | RBAC（菜单权限 + API 权限）、动态路由、超级管理员通配符                        |
| 管理员管理 | CRUD、角色分配、保护规则（超管不可删除/改权限）                                |
| 角色管理   | 自定义角色、菜单/API 权限配置                                                  |
| 题目管理   | 列表/搜索/筛选、新建/编辑/软删除、动态选项、分类关联（仅叶子节点）             |
| 分类管理   | 多维度 + 无限层级树形、「通识」节点自动生命周期管理（不可删除/改名，灰色区分） |
| 用户管理   | 列表/搜索/分页、用户详情、做题历史、偏好分类、状态切换                         |
| 系统日志   | 自动记录增删改 API + 登录日志、类型/操作者/模块/结果/日期筛选、展开行详情      |
| 客户端管理 | SSE 实时监控在线客户端（用户名/游客 + IP + 当前页面）、广播推送（刷新/公告）   |
| Dashboard  | 数据可视化（Chart.js 图表：答题趋势、分类分布、正确率排名）+ 统计卡片          |
| 布局       | Header + Sidebar 折叠 + Tab 历史 + keep-alive + 深色模式                       |

---

## 功能路线图

### Phase 0：工程优化 ✅

- [x] 脚本精简（preview-test.sh / run-e2e.sh）
- [x] 测试优化（单元测试与 E2E 分离，pre-push 只跑单元测试）
- [x] 代码风格统一（monorepo 根目录 Prettier 配置）
- [x] 前端重构（quiz-app 目录结构、组件拆分、状态管理）
- [x] UI 美化（渐变标题、泛光效果、暗色模式、响应式布局）

### Phase 1：管理后台 ✅

- [x] 基础框架（认证、权限、动态路由、Tab 历史、深色模式）
- [x] 管理员/角色/权限管理 CRUD
- [x] 题目管理（列表、编辑、软删除）
- [x] 分类管理（多维度树形，后端 7 个接口，前端递归组件）
- [x] 用户管理（真实后端 6 个接口 + 详情页 + 做题历史 + 偏好查看）
- [x] 系统日志
- [x] 客户端管理（SSE 实时监控 + 广播推送）

### Phase 2：用户系统 ✅

- [x] 共享 UI 组件（BaseDialog/BaseInput/BasePopover/ColumnSelector）
- [x] App 认证系统（useUserStore + AuthDialog + UserDropdown）
- [x] 后端新接口（user-profile、公开分类、题目筛选）+ 叶子节点约束（通识节点自动生命周期）
- [x] 答题历史抽屉 + 分类选择器（Miller Columns 三态选中）+ 登录引导
- [x] E2E 测试 + 边缘情况打磨

### Phase 3：数据分析 + 生产部署 ✅

- [x] Dashboard 数据可视化（Chart.js 图表：答题趋势、分类分布、正确率排名 + 统计卡片）
- [x] 生产环境部署（Nginx 反向代理 + HTTPS Let's Encrypt + PM2 进程管理）
- [x] 域名配置（quiz.illegalscreed.cn / quiz-admin / quiz-api 三个子域名）
- [x] Google Analytics (GA4) 流量统计接入
- [x] 生产安全加固（登录页隐藏测试账号、JWT 环境变量修复）
- [x] 游客登录引导优化（BaseDialog 弹窗 + 仅答错触发）
- [x] 一键部署脚本（`scripts/deploy.sh`，支持 app/admin/backend/all）

### Phase 4：商业化 + 增强（计划中）

- [x] 百度统计接入（国内流量）
- [ ] 广告接入（Google AdSense，已提交审核）
- [ ] 个人学习报告（正确率、弱项分析）
- [ ] 批量导入题目（JSON 文件上传）
- [ ] 热门分类/题目统计
