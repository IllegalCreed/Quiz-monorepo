# pnpm 严格模式下，@iconify-json 需要提升到根 node_modules
# 否则 UnoCSS preset-icons 在生产构建时无法找到图标数据
public-hoist-pattern[]=@iconify-json/*
# @vue/test-utils 内部依赖 @vue/server-renderer（未声明为 peerDep），
# 需要提升才能被 Vitest 的 dep optimization 发现
public-hoist-pattern[]=@vue/server-renderer
