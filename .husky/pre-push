# Run root type-check (uses package.json scripts)
echo "⏳ Running root type-check..."
pnpm run type-check || { echo "Type-check failed"; exit 1; }

# Run package tests via root script

echo "⏳ Running package tests via root 'test' script..."
pnpm run test || { echo "Package tests failed"; exit 1; }

echo "✅ Pre-push checks passed"
