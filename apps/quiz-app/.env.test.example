# Example test env for frontend (copy to .env.test.local and set values)
VITE_API_BASE=http://localhost:10020/api
VITE_MOCK=false

# Optional: override preview port used when running preview
VITE_PORT=10010
