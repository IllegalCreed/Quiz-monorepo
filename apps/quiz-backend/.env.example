# Example env for quiz-backend
# Copy to .env.development.local or .env.production.local and fill in secrets
DATABASE_HOST=localhost
DATABASE_PORT=3306
DATABASE_USERNAME=root
DATABASE_PASSWORD=password
DATABASE_NAME=quiz_dev
# Prisma expects DATABASE_URL in the form:
# mysql://USER:PASSWORD@HOST:PORT/DATABASE
# it's recommended to construct it from the individual variables in runtime or CI
databaseUrlExample=mysql://root:password@localhost:3306/quiz_dev

# Application
PORT=3000
NODE_ENV=development
# Frontend origin allowed for CORS (optional)
FRONTEND_ORIGIN=http://localhost:5173
