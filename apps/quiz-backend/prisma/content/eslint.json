[
  {
    "stem": "ESLint 与 Prettier 在职责上的核心区别是？",
    "explanation": "ESLint 专注代码质量问题（未使用变量、潜在 bug、不合理写法等），Prettier 只负责格式化（缩进、引号、换行等）。两者互补，组合使用：用 ESLint 检查逻辑、用 Prettier 统一外观，通过 `eslint-config-prettier` 关闭与 Prettier 冲突的格式规则。",
    "tags": ["eslint", "prettier", "代码质量"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "入门"]
    ],
    "options": [
      {
        "text": "ESLint 抓代码质量问题，Prettier 只管格式化，二者互补",
        "isCorrect": true,
        "description": "通过 eslint-config-prettier 关闭格式冲突项"
      },
      {
        "text": "Prettier 是 ESLint 的升级版，可完全替代 ESLint",
        "isCorrect": false,
        "description": "Prettier 不检查代码质量问题"
      },
      {
        "text": "ESLint 已废弃，现代项目应只用 Prettier",
        "isCorrect": false,
        "description": "ESLint 仍是 JS 生态最主流的代码质量工具"
      },
      {
        "text": "两者完全重叠，必须二选一",
        "isCorrect": false,
        "description": "覆盖维度不同，可且应同时使用"
      }
    ]
  },
  {
    "stem": "ESLint v9 起默认的配置文件名是？",
    "explanation": "v9 起 **flat config** 成为默认，配置文件名为 `eslint.config.js`（或 `.mjs` / `.cjs` / `.ts` / `.mts` / `.cts`），放在项目根目录。它取代了过去的 `.eslintrc.*`。v10 起 `.eslintrc.*` 彻底停止支持。",
    "tags": ["eslint", "flat-config", "v9"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "入门"]
    ],
    "options": [
      {
        "text": "eslint.config.js（或 .mjs / .cjs / .ts / .mts / .cts）",
        "isCorrect": true,
        "description": "flat config 入口，导出一个配置对象数组"
      },
      {
        "text": ".eslintrc.json",
        "isCorrect": false,
        "description": "v9 已 deprecated，v10 彻底移除"
      },
      {
        "text": "eslint.json",
        "isCorrect": false,
        "description": "并非 ESLint 识别的文件名"
      },
      {
        "text": "package.json 的 eslintConfig 字段",
        "isCorrect": false,
        "description": "这是 eslintrc 时代的写法，flat config 不再支持"
      }
    ]
  },
  {
    "stem": "在 ESLint v9+ 项目中，配置文件里要使用 `eslint:recommended` 这套预设，应该如何写？",
    "explanation": "v9+ flat config 已**完全移除**字符串预设（如 `\"eslint:recommended\"` / `\"eslint:all\"`）。必须从 `@eslint/js` 包显式导入 `js.configs.recommended` 或 `js.configs.all`。`@eslint/js` 是 v9 之后的必装依赖。",
    "tags": ["eslint", "v9", "@eslint/js", "recommended"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "入门"]
    ],
    "options": [
      {
        "text": "import js from \"@eslint/js\"; export default [js.configs.recommended, ...]",
        "isCorrect": true,
        "description": "v9 起的标准写法"
      },
      {
        "text": "export default [{ extends: \"eslint:recommended\" }]",
        "isCorrect": false,
        "description": "字符串预设在 flat config 已移除"
      },
      {
        "text": "export default { extends: [\"eslint:recommended\"] }",
        "isCorrect": false,
        "description": "eslintrc 时代写法，flat config 不再支持"
      },
      {
        "text": "ESLint 默认就开启 recommended，无需任何写法",
        "isCorrect": false,
        "description": "不开启任何配置时 ESLint 不会运行任何规则"
      }
    ]
  },
  {
    "stem": "在 ESLint 规则配置中，下列哪一项**不属于**合法的严重级取值？",
    "explanation": "ESLint 规则严重级有三档：`\"off\"`（或 `0`，关闭）、`\"warn\"`（或 `1`，警告）、`\"error\"`（或 `2`，报错）。`\"fatal\"` 不是合法取值。",
    "tags": ["eslint", "rules", "severity"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "初级"]
    ],
    "options": [
      {
        "text": "\"fatal\"",
        "isCorrect": true,
        "description": "不存在的取值，会被 ESLint 报为配置错误"
      },
      {
        "text": "\"off\" 或 0",
        "isCorrect": false,
        "description": "关闭规则"
      },
      {
        "text": "\"warn\" 或 1",
        "isCorrect": false,
        "description": "警告，不影响退出码"
      },
      {
        "text": "\"error\" 或 2",
        "isCorrect": false,
        "description": "报错，退出码为 1"
      }
    ]
  },
  {
    "stem": "ESLint v9+ 在 `languageOptions.globals` 中声明全局变量时，允许的取值是？",
    "explanation": "v9+ 推荐取值：`readonly`（只读）、`writable`（允许赋值）、`off`（显式取消）。旧的 `true`/`false` 与拼错的 `readable`/`writeable` 仍能用但已 deprecated。",
    "tags": ["eslint", "globals", "languageOptions"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "初级"]
    ],
    "options": [
      {
        "text": "\"readonly\" / \"writable\" / \"off\"",
        "isCorrect": true,
        "description": "v9+ 推荐取值；旧的 true/false 已 deprecated"
      },
      {
        "text": "\"public\" / \"private\" / \"protected\"",
        "isCorrect": false,
        "description": "不是 ESLint 的 globals 取值"
      },
      {
        "text": "\"strict\" / \"loose\" / \"none\"",
        "isCorrect": false,
        "description": "不是 ESLint 的 globals 取值"
      },
      {
        "text": "\"required\" / \"optional\"",
        "isCorrect": false,
        "description": "不是 ESLint 的 globals 取值"
      }
    ]
  },
  {
    "stem": "Flat config 数组中**多个配置对象命中同一个文件**时，最终生效的优先级规则是？",
    "explanation": "按数组顺序遍历，所有命中文件的配置对象逐次合并，**后者覆盖前者**。官方原文：later objects overriding previous objects when there is a conflict。调试用 `eslint --print-config <file>` 看最终结果。",
    "tags": ["eslint", "flat-config", "合并"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "初级"]
    ],
    "options": [
      {
        "text": "按数组顺序合并，后者覆盖前者",
        "isCorrect": true,
        "description": "可用 --print-config 调试最终结果"
      },
      {
        "text": "前者覆盖后者（先声明优先）",
        "isCorrect": false,
        "description": "方向相反"
      },
      {
        "text": "ESLint 自动按规则严重级排序，严的优先",
        "isCorrect": false,
        "description": "完全按数组顺序，不看严重级"
      },
      {
        "text": "随机选一个，行为不可预测",
        "isCorrect": false,
        "description": "数组顺序是确定的"
      }
    ]
  },
  {
    "stem": "在 flat config 中配置一个插件（如 `eslint-plugin-jsdoc`）的正确做法是？",
    "explanation": "v9+ 的 `plugins` 字段是**对象**而非数组：`{ 命名空间: 插件对象 }`。命名空间约定为去掉 `eslint-plugin-` 前缀，规则引用形如 `命名空间/规则名`。",
    "tags": ["eslint", "plugins", "flat-config"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "初级"]
    ],
    "options": [
      {
        "text": "plugins: { jsdoc } —— 对象，key 为命名空间，value 为插件对象",
        "isCorrect": true,
        "description": "规则名引用形如 jsdoc/require-description"
      },
      {
        "text": "plugins: [\"eslint-plugin-jsdoc\"] —— 字符串数组",
        "isCorrect": false,
        "description": "eslintrc 时代写法，flat config 不再支持"
      },
      {
        "text": "extends: [\"plugin:jsdoc/recommended\"] —— extends 字符串",
        "isCorrect": false,
        "description": "flat config 已移除字符串 extends"
      },
      {
        "text": "在 package.json 的 eslintPlugins 字段声明",
        "isCorrect": false,
        "description": "不存在这种字段"
      }
    ]
  },
  {
    "stem": "ESLint 的「全局 ignores」如何在 flat config 中表达？",
    "explanation": "一个对象**只含 `ignores`（最多加 `name`），它就是全局 ignores**，对所有对象生效且能匹配目录。官方推荐用 `eslint/config` 的 `globalIgnores()` helper。局部 ignores 仅当前对象生效、只匹配文件。",
    "tags": ["eslint", "ignores", "globalIgnores"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "初级"]
    ],
    "options": [
      {
        "text": "写一个只含 ignores（可选含 name）的配置对象；推荐用 globalIgnores() helper",
        "isCorrect": true,
        "description": "对所有对象生效，可匹配目录"
      },
      {
        "text": "在 package.json 的 eslintIgnore 字段声明",
        "isCorrect": false,
        "description": "v9+ 已不支持此字段"
      },
      {
        "text": "在每个配置对象里都写一遍 ignores",
        "isCorrect": false,
        "description": "对象级 ignores 只是局部，重复声明繁琐"
      },
      {
        "text": "创建 .eslintignore 文件",
        "isCorrect": false,
        "description": "v9 flat config 不再读 .eslintignore"
      }
    ]
  },
  {
    "stem": "ESLint v9 起，CLI 标志 `--quiet` 的行为发生了什么变化？",
    "explanation": "v8 的 `--quiet` 只过滤输出；**v9 起 `--quiet` 让 warn 级规则根本不执行**。若同时传 `--max-warnings`，warn 规则又会被执行（输出仍抑制）。CI 想阻塞 warn 改用 `--max-warnings=0`。",
    "tags": ["eslint", "v9", "CLI", "--quiet"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "中级"]
    ],
    "options": [
      {
        "text": "v9 起 --quiet 会跳过执行 warn 规则（不只是过滤输出）",
        "isCorrect": true,
        "description": "性能更好；想严格 CI 用 --max-warnings=0"
      },
      {
        "text": "v9 起 --quiet 改为只显示 error，行为同 v8 没变",
        "isCorrect": false,
        "description": "v9 实际有行为变化：warn 规则不再执行"
      },
      {
        "text": "v9 起 --quiet 会让所有 warn 升级为 error",
        "isCorrect": false,
        "description": "不会升级，是不执行"
      },
      {
        "text": "v9 起 --quiet 已被废弃，必须用 --silent",
        "isCorrect": false,
        "description": "--quiet 仍然存在；ESLint 没有 --silent"
      }
    ]
  },
  {
    "stem": "下列对 ESLint v10 升级变更的描述，哪一项**不正确**？",
    "explanation": "v10 真实变更：Node 最低 v20.19/v22.13/v24、`.eslintrc.*` 彻底停止、`/* eslint-env */` 报错、`eslint:recommended` 新增 3 条规则。`@eslint/js` 仍是必装依赖，未被废弃。",
    "tags": ["eslint", "v10", "升级"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "中级"]
    ],
    "options": [
      {
        "text": "@eslint/js 包已被 v10 废弃",
        "isCorrect": true,
        "description": "@eslint/js 在 v10 仍是必装依赖"
      },
      {
        "text": "Node 最低要求抬高到 ^20.19 / ^22.13 / >=24",
        "isCorrect": false,
        "description": "v10 真实变更"
      },
      {
        "text": ".eslintrc.* 配置文件完全停止支持",
        "isCorrect": false,
        "description": "v10 真实变更"
      },
      {
        "text": "/* eslint-env */ 注释现在会报错",
        "isCorrect": false,
        "description": "v10 真实变更，必须改用 globals 包"
      }
    ]
  },
  {
    "stem": "`defineConfig` 与 `globalIgnores` 这两个辅助函数从哪个模块导入？",
    "explanation": "ESLint 自 v9.22.0 起从 `eslint/config` 子路径导出 `defineConfig` 与 `globalIgnores`。前者提供 TS 类型推断与字段校验，后者是写全局 ignores 的语义化 helper。",
    "tags": ["eslint", "defineConfig", "globalIgnores", "v9.22"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "中级"]
    ],
    "options": [
      {
        "text": "eslint/config",
        "isCorrect": true,
        "description": "v9.22.0+ 从该子路径导出"
      },
      {
        "text": "@eslint/js",
        "isCorrect": false,
        "description": "@eslint/js 提供 js.configs.recommended，不包含 defineConfig"
      },
      {
        "text": "eslint",
        "isCorrect": false,
        "description": "顶层 eslint 包没有这两个导出"
      },
      {
        "text": "@eslint/define-config",
        "isCorrect": false,
        "description": "不存在这个官方包"
      }
    ]
  },
  {
    "stem": "ESLint 的 Bulk Suppressions（`--suppress-all` / `--prune-suppressions`）主要用来解决什么问题？",
    "explanation": "存量代码大、想引入新规则但不愿一次性修完时，用 `eslint . --suppress-all` 把当前**所有 error 级违规**快照写入 `eslint-suppressions.json`（视为冻结），后续 CI 只检查新增违规。`--prune-suppressions` 移除已不再适用的条目。",
    "tags": ["eslint", "suppressions", "升级"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "中级"]
    ],
    "options": [
      {
        "text": "冻结存量违规、CI 只检查新增违规，让新规则可以渐进式引入",
        "isCorrect": true,
        "description": "适合升级 ESLint 或开新规则但代码量大的项目"
      },
      {
        "text": "把所有 warn 规则升级为 error",
        "isCorrect": false,
        "description": "那是 --max-warnings=0 的作用，与本机制无关"
      },
      {
        "text": "把所有违规批量自动修复",
        "isCorrect": false,
        "description": "那是 --fix 的作用"
      },
      {
        "text": "永久禁用某条规则，等价于 \"rule\": \"off\"",
        "isCorrect": false,
        "description": "Suppressions 仅冻结现有违规，对新增违规仍生效"
      }
    ]
  },
  {
    "stem": "关于 flat config 的 `extends` 字段（出现在某个配置对象内部），下列哪种说法是正确的？",
    "explanation": "v9 加入的 `extends` 字段允许在**单个对象内部**继承一组预设，元素可为三种：`pluginName/configName` 形式的字符串（引用插件命名配置）、配置对象、配置数组。**不再接受顶层字符串预设** `\"eslint:recommended\"`——此类需 `import` 后展开。",
    "tags": ["eslint", "extends", "flat-config"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "中级"]
    ],
    "options": [
      {
        "text": "在单个对象内部接受三种元素：插件命名配置字符串（`pluginName/configName`）、配置对象、配置数组",
        "isCorrect": true,
        "description": "字符串引用的是「插件内部的命名配置」，不是顶层预设"
      },
      {
        "text": "和 eslintrc 的 extends 一样，接受顶层字符串预设如 \"eslint:recommended\"",
        "isCorrect": false,
        "description": "顶层字符串预设已移除；`eslint:recommended` 需从 `@eslint/js` 导入 `js.configs.recommended`"
      },
      {
        "text": "extends 只能写在顶级数组中，不能嵌入对象",
        "isCorrect": false,
        "description": "恰好相反：它是配置对象的一个字段"
      },
      {
        "text": "extends 字段在 v9 flat config 中已被移除",
        "isCorrect": false,
        "description": "v9 流程中作为新字段加入，并非移除"
      }
    ]
  },
  {
    "stem": "Flat config 中 `files:` 字段省略时，配置对象的作用范围？",
    "explanation": "**省略 `files:` 的配置对全部文件生效**——这是「全局规则」的常用写法（如全局 globals 声明）。**但若想精准只对 ts 生效**，必须显式 `files: ['**/*.ts']`。**特别注意**：`ignores:` 单独成对象（无其它字段）时是「全局 ignore」；与 `files` 一起时是「该配置块的局部 ignore」。",
    "tags": ["eslint", "高级", "flat-config"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "高级"]
    ],
    "options": [
      {
        "text": "对所有文件生效（全局规则）",
        "isCorrect": true,
        "description": "显式 files 才限定范围"
      },
      {
        "text": "对当前目录文件生效",
        "isCorrect": false,
        "description": "无目录推断"
      },
      {
        "text": "对 *.js 生效",
        "isCorrect": false,
        "description": "无后缀推断"
      },
      {
        "text": "报错必须显式 files",
        "isCorrect": false,
        "description": "省略合法"
      }
    ]
  },
  {
    "stem": "Flat config 中加载 React 插件，正确的写法是？",
    "explanation": "**`import reactPlugin from 'eslint-plugin-react'`** + **`plugins: { react: reactPlugin }`** + **`rules: { 'react/no-unused-prop-types': 'error' }`**。必须 import 后放进 plugins，不支持字符串。",
    "tags": ["eslint", "高级", "plugin"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "高级"]
    ],
    "options": [
      {
        "text": "import 后放入 plugins 对象，rules 用 `react/xxx` 引用",
        "isCorrect": true,
        "description": "v9 flat config 标准"
      },
      {
        "text": "`plugins: ['react']`（字符串数组）",
        "isCorrect": false,
        "description": "legacy 写法不支持"
      },
      {
        "text": "`extends: ['plugin:react/recommended']`",
        "isCorrect": false,
        "description": "legacy extends 不支持"
      },
      {
        "text": "在 package.json 的 eslintConfig.plugins 声明",
        "isCorrect": false,
        "description": "flat config 不用此"
      }
    ]
  },
  {
    "stem": "Flat config 中 `linterOptions.reportUnusedDisableDirectives` 的作用？",
    "explanation": "**对未被任何规则使用的 `/* eslint-disable */` 注释报错**——例：写了 `/* eslint-disable no-console */` 但本文件未触发 no-console → 报警「这条 disable 注释是无效的」。清理过期 disable 注释。值可 `error / warn / off`。",
    "tags": ["eslint", "高级", "flat-config"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "高级"]
    ],
    "options": [
      {
        "text": "对未被使用的 eslint-disable 注释报警",
        "isCorrect": true,
        "description": "清理过期注释"
      },
      {
        "text": "禁用所有 eslint-disable 注释",
        "isCorrect": false,
        "description": "不禁用，只检测无效"
      },
      {
        "text": "把 disable 注释自动改成 enable",
        "isCorrect": false,
        "description": "无自动转换"
      },
      {
        "text": "报告 CLI --no-warn-ignored 类似行为",
        "isCorrect": false,
        "description": "另一个 option"
      }
    ]
  },
  {
    "stem": "Flat config 中 `processor:` 字段的典型场景？",
    "explanation": "**对非 JS 文件预处理后再 lint**——例：`eslint-plugin-markdown` processor 把 MD 代码块抽出来 lint。又如 Vue 文件需 `vue-eslint-parser` + processor 拆 SFC 块。flat config 写 `processor: 'plugin/processor-name'`。",
    "tags": ["eslint", "高级", "processor"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "高级"]
    ],
    "options": [
      {
        "text": "对非 JS 文件预处理后再 lint（Markdown/Vue SFC 等）",
        "isCorrect": true,
        "description": "拆出代码块/SFC 块分别 lint"
      },
      {
        "text": "处理 CLI 命令行参数",
        "isCorrect": false,
        "description": "无关 CLI"
      },
      {
        "text": "替代 parser",
        "isCorrect": false,
        "description": "preprocessor 是文件级别"
      },
      {
        "text": "用于格式化代码",
        "isCorrect": false,
        "description": "格式化是 Prettier"
      }
    ]
  },
  {
    "stem": "ESLint 规则的「Fixable」类别中，「`auto-fixable`」与「`suggestion`」的区别？",
    "explanation": "**`auto-fixable`**（fixable: 'code'/'whitespace'）：`eslint --fix` 自动应用。**`suggestion`**（hasSuggestions: true）：编辑器/IDE 通过 LSP 提示候选 fix，**用户手动选**——适合「多种修法」或「修复会改变行为」。CLI --fix 不自动应用 suggestion。",
    "tags": ["eslint", "高级", "rule"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "高级"]
    ],
    "options": [
      {
        "text": "auto-fix 由 --fix 自动应用；suggestion 由用户在编辑器手动选",
        "isCorrect": true,
        "description": "suggestion 适合多种修法/有副作用场景"
      },
      {
        "text": "两者等价",
        "isCorrect": false,
        "description": "应用模型不同"
      },
      {
        "text": "suggestion 仅在 v10 才支持",
        "isCorrect": false,
        "description": "v8 就支持"
      },
      {
        "text": "--fix 默认包含 suggestion",
        "isCorrect": false,
        "description": "需 --fix-type suggestion"
      }
    ]
  },
  {
    "stem": "`@typescript-eslint` 的 type-aware rule 与普通 rule 的差别？",
    "explanation": "**type-aware rule**（如 `no-floating-promises`）：需 `parserOptions.project` 加载 TS 类型信息后再 lint。**普通 rule**：仅看 AST，速度快。type-aware 慢 5-10 倍但能 catch 「未 await 的 Promise」等需类型的错。",
    "tags": ["eslint", "高级", "typescript"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "高级"]
    ],
    "options": [
      {
        "text": "type-aware 需 parserOptions.project 加载类型，慢 5-10 倍",
        "isCorrect": true,
        "description": "能 catch no-floating-promises 等"
      },
      {
        "text": "完全等价",
        "isCorrect": false,
        "description": "执行模型不同"
      },
      {
        "text": "type-aware 不需要 tsconfig",
        "isCorrect": false,
        "description": "必须配 project"
      },
      {
        "text": "普通 rule 不能用在 TS 文件",
        "isCorrect": false,
        "description": "都能用"
      }
    ]
  },
  {
    "stem": "`@typescript-eslint` v8 起推荐的 `projectService` 与 `project` 配置的区别？",
    "explanation": "**`projectService: true`** 是 v8 推荐方式——自动发现 tsconfig（无需手动列举），且对大 monorepo 性能更好（按需加载）。**`project: ['./tsconfig.json']`**：手动指定 tsconfig 列表，初始化时全部加载，monorepo 多个 tsconfig 时启动慢。新项目用 projectService。",
    "tags": ["eslint", "高级", "typescript"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "高级"]
    ],
    "options": [
      {
        "text": "projectService 自动发现 tsconfig 且按需加载，性能更好",
        "isCorrect": true,
        "description": "v8 推荐"
      },
      {
        "text": "完全等价",
        "isCorrect": false,
        "description": "加载模型不同"
      },
      {
        "text": "projectService 必须配 path",
        "isCorrect": false,
        "description": "true 即自动发现"
      },
      {
        "text": "project 已废弃",
        "isCorrect": false,
        "description": "可用但不推荐"
      }
    ]
  },
  {
    "stem": "ESLint v9 起对单文件 lint 速度有显著提升的特性是？",
    "explanation": "**`--cache`** 默认启用（v9 起 cache 命中条件优化），**重复 lint 未变文件直接跳过**。同时 v9 把 flat config 解析层重写，启动开销也降低。对大型仓库的 CI lint 全量扫描，加 `--cache --cache-location node_modules/.cache/eslint` 加速明显。",
    "tags": ["eslint", "高级", "performance"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "高级"]
    ],
    "options": [
      {
        "text": "`--cache` 默认启用 + flat config 解析优化",
        "isCorrect": true,
        "description": "未变文件直接跳过"
      },
      {
        "text": "改用 Rust 实现",
        "isCorrect": false,
        "description": "仍是 Node"
      },
      {
        "text": "并行规则执行",
        "isCorrect": false,
        "description": "未推出"
      },
      {
        "text": "禁用所有 plugin",
        "isCorrect": false,
        "description": "不会"
      }
    ]
  },
  {
    "stem": "ESLint `--cache-strategy` 的两种取值差别？",
    "explanation": "**`metadata`**（默认）：用文件 mtime + size 判断变更——快但 git 切分支后 mtime 变所有文件 cache miss。**`content`**：用文件内容 hash 判断——准确但每次要算 hash，初始扫描慢。**CI 场景推荐 `content`**（避免 mtime 误判），本地 `metadata` 足够快。",
    "tags": ["eslint", "高级", "cache"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "高级"]
    ],
    "options": [
      {
        "text": "metadata 看 mtime 快但易误判；content 看内容 hash 准但慢",
        "isCorrect": true,
        "description": "CI 推荐 content"
      },
      {
        "text": "完全等价",
        "isCorrect": false,
        "description": "判断方式不同"
      },
      {
        "text": "content 已废弃",
        "isCorrect": false,
        "description": "v9 仍支持"
      },
      {
        "text": "metadata 不可用于 CI",
        "isCorrect": false,
        "description": "可用，但不准"
      }
    ]
  },
  {
    "stem": "lint-staged 配合 ESLint 的最佳实践命令？",
    "explanation": "```json\n{\n  \"editor.codeActionsOnSave\": {\n    \"source.fixAll.eslint\": \"explicit\"\n  }\n}\n```\n值用字符串 `\"explicit\"`（替代旧 `true`，VS Code 1.85+），**只对保存且无错误时触发**。`\"always\"` 任何保存都触发可能扰乱编辑流。",
    "tags": ["eslint", "中级", "integration"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "中级"]
    ],
    "options": [
      {
        "text": "`eslint --fix --cache --max-warnings=0`（让 lint-staged 传文件）",
        "isCorrect": true,
        "description": "不要写路径让全扫描"
      },
      {
        "text": "`eslint .`",
        "isCorrect": false,
        "description": "无视 lint-staged 过滤"
      },
      {
        "text": "`eslint src/ --fix`",
        "isCorrect": false,
        "description": "扫未 stage 文件"
      },
      {
        "text": "`eslint --no-fix`",
        "isCorrect": false,
        "description": "禁了自动修复无意义"
      }
    ]
  },
  {
    "stem": "VS Code ESLint 插件保存时自动修复，正确的 `settings.json` 配置是？",
    "explanation": "```json\n{\n  \"editor.codeActionsOnSave\": {\n    \"source.fixAll.eslint\": \"explicit\"\n  }\n}\n```\n关键点：值用字符串 `\"explicit\"`（替代旧的 `true`，VS Code 1.85+），且**只对保存且无错误时触发**。`\"always\"` 任何保存都触发可能扰乱编辑流。",
    "tags": ["eslint", "中级", "integration"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "中级"]
    ],
    "options": [
      {
        "text": "`\"source.fixAll.eslint\": \"explicit\"`",
        "isCorrect": true,
        "description": "v1.85+ 推荐写法"
      },
      {
        "text": "`\"source.fixAll.eslint\": true`",
        "isCorrect": false,
        "description": "旧布尔写法已废弃"
      },
      {
        "text": "`\"eslint.autoFix\": true`",
        "isCorrect": false,
        "description": "无此配置项"
      },
      {
        "text": "`\"editor.formatOnSave\": true`",
        "isCorrect": false,
        "description": "那触发 formatter"
      }
    ]
  },
  {
    "stem": "Prettier 与 ESLint 同时启用，如何避免规则冲突？",
    "explanation": "**Node.js 20 LTS**（v9 是 18+）。v10 把目标 Node 提到 20，配合 ES2024 语法。在 Node 18 上运行 ESLint v10 会启动报错。生产用 v9 还是 v10：v10 在 2025 年底发布，Node 20 升级前用 v9。",
    "tags": ["eslint", "中级", "integration"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "中级"]
    ],
    "options": [
      {
        "text": "用 eslint-config-prettier 关闭格式化类规则",
        "isCorrect": true,
        "description": "不要用 eslint-plugin-prettier"
      },
      {
        "text": "用 eslint-plugin-prettier 把 Prettier 当规则跑",
        "isCorrect": false,
        "description": "性能差且冗余"
      },
      {
        "text": "禁用 ESLint，只用 Prettier",
        "isCorrect": false,
        "description": "质量类规则需要 ESLint"
      },
      {
        "text": "禁用 Prettier，只用 ESLint",
        "isCorrect": false,
        "description": "Prettier 格式化更强"
      }
    ]
  },
  {
    "stem": "ESLint v10 起，Node 版本最低要求是？",
    "explanation": "**Node.js 20 LTS**（v9 是 18+）。v10 把目标 Node 提到 20，配合 ES2024 语法。在 Node 18 上运行 ESLint v10 会启动报错。**生产用 v9 还是 v10**：v10 在 2025 年底发布，Node 20 升级前用 v9。",
    "tags": ["eslint", "中级", "v10"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "中级"]
    ],
    "options": [
      {
        "text": "Node 20 LTS",
        "isCorrect": true,
        "description": "v9 是 Node 18"
      },
      {
        "text": "Node 18",
        "isCorrect": false,
        "description": "v9 的要求"
      },
      {
        "text": "Node 22",
        "isCorrect": false,
        "description": "未要求 22"
      },
      {
        "text": "Node 16",
        "isCorrect": false,
        "description": "v8 时代"
      }
    ]
  },
  {
    "stem": "ESLint v10 移除的 legacy eslintrc 兼容性意味着？",
    "explanation": "**`.eslintrc.*` 文件完全不再被识别**——v9 还能通过 `ESLINT_USE_FLAT_CONFIG=false` 兼容旧 eslintrc。v10 完全废除该环境变量。**项目升级 v10 前必须迁移到 flat config**，否则 ESLint 找不到配置直接报错退出。",
    "tags": ["eslint", "中级", "v10"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "中级"]
    ],
    "options": [
      {
        "text": "`.eslintrc.*` 完全不再支持，必须 flat config",
        "isCorrect": true,
        "description": "v9 兼容 env 也废了"
      },
      {
        "text": "eslintrc 仍能用但报 warning",
        "isCorrect": false,
        "description": "v10 完全废除"
      },
      {
        "text": "仅 .eslintrc.js 不支持，json 还能用",
        "isCorrect": false,
        "description": "全废"
      },
      {
        "text": "可用 v9 兼容模式",
        "isCorrect": false,
        "description": "v10 移除该模式"
      }
    ]
  },
  {
    "stem": "想为公司内部多项目共享一套 ESLint 配置，flat config 模式下推荐做法？",
    "explanation": "**发布 npm 包导出 flat config 数组**：`export default [{ rules, plugins }, ...]`。下游 `import shared from '@company/eslint-config'` 展开到 `eslint.config.js`。比 v8「eslint-config-xxx + extends 字符串」清晰多了。",
    "tags": ["eslint", "高级", "shared-config"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "高级"]
    ],
    "options": [
      {
        "text": "发布 npm 包 export flat config 数组，下游 import 展开",
        "isCorrect": true,
        "description": "替代 v8 extends 字符串"
      },
      {
        "text": "推送到 git 仓库直接 clone 引用",
        "isCorrect": false,
        "description": "无法版本管理"
      },
      {
        "text": "把 eslint.config.js 复制到每个项目",
        "isCorrect": false,
        "description": "无法统一升级"
      },
      {
        "text": "用 .eslintrc 写 extends: company-config",
        "isCorrect": false,
        "description": "flat config 不支持"
      }
    ]
  },
  {
    "stem": "ESLint 自定义规则中 `meta.fixable` 与 `meta.hasSuggestions` 的差别？",
    "explanation": "**`meta.fixable`**：rule 提供 `fixer.replaceText()` 自动修复，CLI `--fix` 应用。值需 `'code'` 或 `'whitespace'`。**`meta.hasSuggestions: true`**：rule 用 `context.report({ suggest })` 候选——用户编辑器手选，--fix 不自动应用。",
    "tags": ["eslint", "高级", "custom-rule"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "高级"]
    ],
    "options": [
      {
        "text": "fixable 自动应用；hasSuggestions 用户手选",
        "isCorrect": true,
        "description": "应用方式不同"
      },
      {
        "text": "完全等价",
        "isCorrect": false,
        "description": "应用模型不同"
      },
      {
        "text": "hasSuggestions 已废弃",
        "isCorrect": false,
        "description": "v8 起支持"
      },
      {
        "text": "fixable 只能修空白",
        "isCorrect": false,
        "description": "可修代码"
      }
    ]
  },
  {
    "stem": "想知道某文件实际应用了哪些规则配置，正确命令是？",
    "explanation": "**`eslint --print-config <file>`**——输出该文件最终合并后的所有配置（rules / parser / plugins），常用于「调试为什么某规则没生效」「flat config 多个对象合并后实际什么样」。输出 JSON，常配 `| jq` 过滤。",
    "tags": ["eslint", "中级", "cli"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "中级"]
    ],
    "options": [
      {
        "text": "`eslint --print-config <file>`",
        "isCorrect": true,
        "description": "输出合并后的完整配置"
      },
      {
        "text": "`eslint --debug`",
        "isCorrect": false,
        "description": "输出运行时日志，非配置"
      },
      {
        "text": "`eslint --show-config`",
        "isCorrect": false,
        "description": "无此参数"
      },
      {
        "text": "`eslint config <file>`",
        "isCorrect": false,
        "description": "无此子命令"
      }
    ]
  },
  {
    "stem": "`eslint --debug` 主要输出？",
    "explanation": "**详细运行时日志**——配置文件加载路径、parser 解析时间、各 plugin 加载、每个文件的规则触发情况等。常用于「**为什么这个 file 没被 lint**」「**flat config 哪个对象命中了**」。输出非常啰嗦，最好配 `2>&1 | grep` 过滤。",
    "tags": ["eslint", "中级", "cli"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "中级"]
    ],
    "options": [
      {
        "text": "详细运行时日志（配置加载、parser、规则触发）",
        "isCorrect": true,
        "description": "调试为什么没生效"
      },
      {
        "text": "等价 --print-config",
        "isCorrect": false,
        "description": "前者运行时日志，后者合并配置"
      },
      {
        "text": "输出 AST",
        "isCorrect": false,
        "description": "需另用 espree"
      },
      {
        "text": "等价 --quiet",
        "isCorrect": false,
        "description": "相反"
      }
    ]
  },
  {
    "stem": "ESLint 报「ParserError: Unexpected token」的常见原因？",
    "explanation": "**详细运行时日志**——配置文件加载路径、parser 解析时间、各 plugin 加载、每个文件的规则触发情况。常用于「为什么这个 file 没被 lint」「flat config 哪个对象命中了」。输出非常啰嗦，最好配 `2>&1 | grep` 过滤。",
    "tags": ["eslint", "中级", "error"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "中级"]
    ],
    "options": [
      {
        "text": "默认 parser 不支持 TS/JSX，需装/配置专门 parser",
        "isCorrect": true,
        "description": "@typescript-eslint/parser 等"
      },
      {
        "text": "代码确实语法错",
        "isCorrect": false,
        "description": "也可能但先排查 parser"
      },
      {
        "text": "Node 版本太低",
        "isCorrect": false,
        "description": "ESLint 自带 parser"
      },
      {
        "text": "缺 .eslintrc",
        "isCorrect": false,
        "description": "无关配置文件"
      }
    ]
  },
  {
    "stem": "`/* eslint-disable rule-name */` 与 `/* eslint-disable-next-line rule-name */` 的区别？",
    "explanation": "**`eslint-disable`**：从该行起到文件末（或 `eslint-enable` 之前）整段禁用规则。**`eslint-disable-next-line`**：**仅下一行**禁用。**`eslint-disable-line`** 写在行末：仅当前行禁用。**最佳实践**：用 next-line 精准禁用一行，避免大段 disable 漏掉真问题。",
    "tags": ["eslint", "入门", "directive"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "入门"]
    ],
    "options": [
      {
        "text": "前者整段禁用至 enable；后者仅下一行禁用",
        "isCorrect": true,
        "description": "next-line 精准推荐"
      },
      {
        "text": "完全等价",
        "isCorrect": false,
        "description": "范围不同"
      },
      {
        "text": "disable-next-line 已废弃",
        "isCorrect": false,
        "description": "推荐写法"
      },
      {
        "text": "disable 仅对单行生效",
        "isCorrect": false,
        "description": "整段"
      }
    ]
  },
  {
    "stem": "ESLint 报「No files matching pattern were found」最常见原因？",
    "explanation": "**`eslint-disable`**：从该行起到文件末（或 `eslint-enable` 之前）整段禁用。**`eslint-disable-next-line`**：仅下一行禁用。**`eslint-disable-line`** 写行末：仅当前行禁用。**最佳实践**：用 next-line 精准禁用一行，避免大段 disable 漏掉真问题。",
    "tags": ["eslint", "入门", "config"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "入门"]
    ],
    "options": [
      {
        "text": "v9 默认只扫 .js/.mjs/.cjs，需在 files 显式声明其它后缀",
        "isCorrect": true,
        "description": "v8 默认 .ext，v9 改了"
      },
      {
        "text": "ESLint 装错版本",
        "isCorrect": false,
        "description": "更可能配置问题"
      },
      {
        "text": "项目无 .eslintrc",
        "isCorrect": false,
        "description": "flat config 不要"
      },
      {
        "text": "Node 版本太低",
        "isCorrect": false,
        "description": "无关 Node"
      }
    ]
  },
  {
    "stem": "把 `error` 规则改成 `warn` 但仍想阻断 CI，CLI 该加什么？",
    "explanation": "**`--max-warnings=0`**——把 warn 数量上限设为 0，超 0 个 warning 退出码非 0 → CI fail。这样既能在编辑器中以「warning」黄色提示而非「error」红线（避免心理负担），又保留 CI 阻断能力。",
    "tags": ["eslint", "入门", "cli"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "入门"]
    ],
    "options": [
      {
        "text": "`--max-warnings=0`",
        "isCorrect": true,
        "description": "warn 也阻断"
      },
      {
        "text": "`--strict`",
        "isCorrect": false,
        "description": "无此参数"
      },
      {
        "text": "`--treat-warnings-as-errors`",
        "isCorrect": false,
        "description": "无此参数"
      },
      {
        "text": "改回 error 严重级",
        "isCorrect": false,
        "description": "失去 warn 的好处"
      }
    ]
  },
  {
    "stem": "Flat config 中 `languageOptions.ecmaVersion: 'latest'` 与 `2024` 的差别？",
    "explanation": "**`'latest'`** 自动跟随 ESLint 当前最高支持版本——可能随 ESLint 升级而提升。**`2024`** 钉死该版本，未来 ESLint 升级也仍按 2024 解析。**新项目推荐 'latest'**（跟随生态）；稳定库可钉死版本避免升级意外。",
    "tags": ["eslint", "高级", "config"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "高级"]
    ],
    "options": [
      {
        "text": "latest 跟随 ESLint 最高版本；2024 钉死该版本",
        "isCorrect": true,
        "description": "新项目 latest"
      },
      {
        "text": "完全等价",
        "isCorrect": false,
        "description": "版本绑定方式不同"
      },
      {
        "text": "latest 已废弃",
        "isCorrect": false,
        "description": "推荐写法"
      },
      {
        "text": "2024 不支持 top-level await",
        "isCorrect": false,
        "description": "支持"
      }
    ]
  },
  {
    "stem": "`globalIgnores` 与配置对象单独 `ignores:` 的差别？",
    "explanation": "**`globalIgnores(['dist/**', 'coverage/**'])`**：明确语义，文件被全局忽略——任何配置都不处理。**单独 `{ ignores: [...] }`**（无其它字段）：旧式写法，等价 globalIgnores。**`{ files: [...], ignores: [...] }`**：仅在当前配置块内忽略——其它块仍处理这些文件。",
    "tags": ["eslint", "高级", "ignore"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "高级"]
    ],
    "options": [
      {
        "text": "globalIgnores 是辅助函数，等价于单独 ignores 配置对象",
        "isCorrect": true,
        "description": "都是全局忽略"
      },
      {
        "text": "完全不同的概念",
        "isCorrect": false,
        "description": "等价"
      },
      {
        "text": "globalIgnores 不支持 glob",
        "isCorrect": false,
        "description": "支持"
      },
      {
        "text": "单独 ignores 已废弃",
        "isCorrect": false,
        "description": "仍可用"
      }
    ]
  },
  {
    "stem": "ESLint Bulk Suppressions 的工作机制？",
    "explanation": "**`--suppress-all`** 把当前所有 lint 错误记录到 `.eslint-suppressions.json`——后续 lint 自动跳过这些已记录的错误（**「快照式抑制」**）。新增代码触发错误才会报。`--prune-suppressions` 清理已修复但仍在文件中的记录。**适合**：升级规则后老仓库立刻不被淹没。",
    "tags": ["eslint", "高级", "bulk-suppress"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "高级"]
    ],
    "options": [
      {
        "text": "把当前错误快照到文件，后续 lint 跳过这些已记录错误",
        "isCorrect": true,
        "description": "升级规则时控制爆炸"
      },
      {
        "text": "等价 eslint-disable 批量加",
        "isCorrect": false,
        "description": "不修改源码"
      },
      {
        "text": "永久禁用全部规则",
        "isCorrect": false,
        "description": "仅快照错误"
      },
      {
        "text": "v10 才支持",
        "isCorrect": false,
        "description": "v9 后期引入"
      }
    ]
  },
  {
    "stem": "`eslint --rulesdir` 标志的用途？已废弃没？",
    "explanation": "**已废弃**——v9 起完全移除。原用于「指定本地目录加载自定义规则」。**新方案**：把规则打包成 plugin（`{ rules: { my-rule: {...} } }`），在 flat config `plugins:` 注册。`local-rules` 风格的小项目自定义规则也走 plugin 形式。",
    "tags": ["eslint", "高级", "deprecated"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "高级"]
    ],
    "options": [
      {
        "text": "v9 已移除，改用 plugin 注册自定义规则",
        "isCorrect": true,
        "description": "本地规则走 plugin 形式"
      },
      {
        "text": "仍是推荐用法",
        "isCorrect": false,
        "description": "已移除"
      },
      {
        "text": "v10 才移除",
        "isCorrect": false,
        "description": "v9 就移除"
      },
      {
        "text": "改名为 --plugin-dir",
        "isCorrect": false,
        "description": "无此参数"
      }
    ]
  },
  {
    "stem": "ESLint 报「Plugin xxx not found」flat config 下的原因？",
    "explanation": "**flat config 必须 import 后放进 `plugins:` 对象**——而非旧 `plugins: ['xxx']` 字符串数组（v9 完全不支持字符串引用）。检查：① npm 是否装了该包；② config 是否 `import xxx from 'eslint-plugin-xxx'`；③ 是否写进 `plugins: { xxx }`。",
    "tags": ["eslint", "中级", "error"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "中级"]
    ],
    "options": [
      {
        "text": "flat config 没 import 后放进 plugins 对象",
        "isCorrect": true,
        "description": "v9 不支持字符串引用"
      },
      {
        "text": "npm 未装该包（最常见）",
        "isCorrect": false,
        "description": "可能但 import 错更常见"
      },
      {
        "text": "需在 .eslintrc 注册",
        "isCorrect": false,
        "description": "flat config 不要 eslintrc"
      },
      {
        "text": "Plugin 名要带 eslint-plugin- 前缀",
        "isCorrect": false,
        "description": "import 时已是全名"
      }
    ]
  },
  {
    "stem": "ESLint flat config 中多个配置对象命中同一文件做级联合并时，`languageOptions.globals` 与 `rules` 两个字段的合并方式分别是？",
    "explanation": "官方原文：configuration objects are merged with later objects overriding previous objects when there is a conflict。但「合并」对不同字段粒度不同：`languageOptions.globals` 这类对象会逐键深合并（两个对象的 globals 累加，如 `**/*.js` 给 `MY_CUSTOM_GLOBAL`、`tests/**/*.js` 再叠加 `it`/`describe`，两者并存）；而 `rules` 中某条规则一旦在后面对象重新声明，是「整条替换」——后者的 severity 与选项数组完全顶掉前者，**不会把选项数组逐项合并**。这是排查「为什么我在第二个块只想改 severity，结果把第一个块给的规则选项也清空了」的关键。",
    "tags": ["eslint", "专家", "flat-config", "merge"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "专家"]
    ],
    "options": [
      {
        "text": "globals 逐键深合并（累加并存）；rules 中同名规则整条替换（severity + 选项一起被顶掉，不逐项合并选项）",
        "isCorrect": true,
        "description": "官方示例：tests 目录的 globals 在通用 globals 上叠加；而重声明规则会整体覆盖"
      },
      {
        "text": "两者都逐键深合并，rules 的选项数组会逐项合并",
        "isCorrect": false,
        "description": "rules 是整条替换，不会逐项合并选项数组"
      },
      {
        "text": "两者都整条替换，后一个对象的 globals 会清空前面所有 globals",
        "isCorrect": false,
        "description": "globals 是合并累加，不是清空"
      },
      {
        "text": "globals 整条替换，rules 逐项合并",
        "isCorrect": false,
        "description": "方向恰好相反"
      }
    ]
  },
  {
    "stem": "在 ESLint flat config 的 `files` 字段里写 `files: [[\"src/**\", \"**/*.ts\"]]`（注意是数组套数组），它的匹配语义是？",
    "explanation": "flat config 的 `files` 顶层数组各元素之间是「或」（命中任一即匹配），但当某个元素本身又是一个字符串数组时，这层内部数组表示「与」——官方说明：a file is both inside of the `src` directory and also ends with `.js`（此处即「既在 `src/` 目录下，又以 `.ts` 结尾」才命中）。因此 `[[\"src/**\", \"**/*.ts\"]]` 只命中 `src` 下的 `.ts` 文件，而 `[\"src/**\", \"**/*.ts\"]`（无内层嵌套）则是「`src` 下任意文件，或任意 `.ts` 文件」。",
    "tags": ["eslint", "专家", "files", "glob"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "专家"]
    ],
    "options": [
      {
        "text": "内层数组是「与」：只命中既在 `src/` 下、又以 `.ts` 结尾的文件",
        "isCorrect": true,
        "description": "顶层数组元素之间是「或」，元素内嵌套数组是「与」"
      },
      {
        "text": "和 `[\"src/**\", \"**/*.ts\"]` 完全等价，都是「或」",
        "isCorrect": false,
        "description": "嵌套层引入了「与」语义，二者不等价"
      },
      {
        "text": "嵌套数组是非法写法，ESLint 会报配置错误",
        "isCorrect": false,
        "description": "这是官方支持的 AND 语法，合法"
      },
      {
        "text": "内层数组表示「先匹配 src，匹配不到再退化到 .ts」的回退顺序",
        "isCorrect": false,
        "description": "没有回退语义，是逻辑与"
      }
    ]
  },
  {
    "stem": "ESLint v9.x 新增的 `basePath` 配置字段，配合 `extends` 时最大的便利是什么？",
    "explanation": "`basePath` 让一个配置对象（及其相对 glob）作用于某个子目录（含其子目录），路径解析以该子目录为基准。它的关键便利是：当一个对象用 `extends` 继承一组配置数组并想整体限定到某子目录时，只需在外层写一次 `basePath`，被 `extends` 进来的所有配置都会一并落到该子目录，无需在每个子配置里重复 `files: ['packages/foo/**']` 这类前缀。这在 monorepo 里给某个包套用一整套预设时尤其省事。",
    "tags": ["eslint", "专家", "basePath", "monorepo"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "专家"]
    ],
    "options": [
      {
        "text": "只需写一次 `basePath`，被 `extends` 进来的整组配置都自动限定到该子目录",
        "isCorrect": true,
        "description": "省去在每个子配置重复目录前缀，monorepo 给单包套预设很方便"
      },
      {
        "text": "`basePath` 会自动忽略该目录外的所有文件，等价全局 ignores",
        "isCorrect": false,
        "description": "它是限定作用范围，不是全局忽略其它目录"
      },
      {
        "text": "`basePath` 让相对路径以 `process.cwd()` 解析而非配置文件位置",
        "isCorrect": false,
        "description": "它是把基准设为指定子目录，而非 cwd"
      },
      {
        "text": "`basePath` 允许在 flat config 里继续使用字符串预设 `eslint:recommended`",
        "isCorrect": false,
        "description": "与字符串预设无关，那在 flat config 仍需 import"
      }
    ]
  },
  {
    "stem": "编写 ESLint 自定义规则时，若规则会从配置里读取选项（`context.options`），但 `meta.schema` 完全没声明，会发生什么？",
    "explanation": "官方明确：For rules that don't specify a `meta.schema` property, ESLint throws errors when any options are passed。也就是说「不写 schema」不等于「接受任意选项」，而是「不接受任何选项」——一旦用户给该规则传了选项，ESLint 在校验阶段直接报错。要接受选项必须显式声明 `meta.schema`（JSON Schema 数组）。若想完全不校验、放行任意选项，可显式写 `meta.schema: false`。这也是为什么第三方规则报「该规则不接受选项」时，根因往往是规则作者漏写了 schema。",
    "tags": ["eslint", "专家", "custom-rule", "meta.schema"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "专家"]
    ],
    "options": [
      {
        "text": "只要给该规则传了选项，ESLint 在校验阶段直接抛错（没声明 schema = 不接受任何选项）",
        "isCorrect": true,
        "description": "想接受选项须显式 meta.schema；放行任意选项用 meta.schema: false"
      },
      {
        "text": "ESLint 会默默接受并把选项原样传给规则，不做任何校验",
        "isCorrect": false,
        "description": "无 schema 时传选项是直接报错，不是默默放行"
      },
      {
        "text": "ESLint 自动从规则代码里推断出一个 schema 来校验",
        "isCorrect": false,
        "description": "不存在自动推断 schema 的机制"
      },
      {
        "text": "选项会被静默丢弃，规则按默认值运行且无任何提示",
        "isCorrect": false,
        "description": "不是静默丢弃，而是抛配置错误"
      }
    ]
  },
  {
    "stem": "在 ESLint flat config 中通过 `plugins` 注册自定义插件 / 自定义 processor 时，官方强调插件对象上务必带 `meta`（`name` + `version`）的直接原因是？",
    "explanation": "迁移到 flat config 后，ESLint 不再能从包名推断插件元信息，必须由插件对象显式提供 `meta: { name, version }`。官方原文指出这对 `--cache` 与 `--print-config` 至关重要：缓存需要插件标识与版本来判断缓存是否失效，`--print-config` 需要它来标识配置来源；processor 同理也应各自带 `meta`。缺失 `meta` 会导致缓存命中判断不可靠或调试输出无法定位插件，并非只是「美观」。",
    "tags": ["eslint", "专家", "plugin", "meta"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "专家"]
    ],
    "options": [
      {
        "text": "flat config 无法再从包名推断元信息，`meta` 的 name/version 是 `--cache` 缓存失效判断与 `--print-config` 来源标识所必需",
        "isCorrect": true,
        "description": "processor 也应各自带 meta，否则缓存/调试不可靠"
      },
      {
        "text": "`meta` 仅用于在文档站展示规则说明，对运行无影响",
        "isCorrect": false,
        "description": "它实际影响 --cache 与 --print-config 的正确性"
      },
      {
        "text": "`meta` 用于声明插件依赖的 ESLint 版本范围，缺失会拒绝加载",
        "isCorrect": false,
        "description": "name/version 是插件自身标识，不是依赖范围声明"
      },
      {
        "text": "`meta` 决定插件规则的默认 severity，缺失则全部按 error 运行",
        "isCorrect": false,
        "description": "severity 由用户配置决定，与 meta 无关"
      }
    ]
  },
  {
    "stem": "团队从 `eslint-config-foo`（导出 flat config 数组）用 ESLint 的 `extends` 引入时，借助 `defineConfig` 为这些被继承的配置统一加 `name` 前缀有什么实际价值？",
    "explanation": "`defineConfig()` 处理 `extends` 时，会把被继承配置的 `name` 与当前对象的 `name` 用 `/` 拼接，形成层级化命名（如 `my-app/foo/recommended`）。`name` 会出现在错误信息与 config inspector 中，用于标识「究竟是哪一个配置对象命中了当前文件」。官方虽不强制 `name` 唯一，但推荐唯一以免混淆。当多个共享预设叠加、出现规则冲突或排查某规则来源时，清晰的层级 `name` 能直接定位到具体来源，是调试大型组合配置的利器。",
    "tags": ["eslint", "专家", "defineConfig", "extends"],
    "categories": [
      ["技术方向", "ESLint"],
      ["难度", "专家"]
    ],
    "options": [
      {
        "text": "被继承配置的 name 会与当前 name 以 `/` 拼成层级名，出现在报错与 config inspector 里，便于定位规则来源",
        "isCorrect": true,
        "description": "官方推荐 name 唯一以免混淆；调试组合配置时极有用"
      },
      {
        "text": "`name` 会改变配置的合并优先级，前缀越长优先级越高",
        "isCorrect": false,
        "description": "优先级只看数组顺序，与 name 无关"
      },
      {
        "text": "`name` 是运行时强制唯一的，重复会导致 ESLint 启动报错",
        "isCorrect": false,
        "description": "官方明确不在运行时强制唯一，只是推荐唯一"
      },
      {
        "text": "加 `name` 前缀后才能让被继承的规则生效，否则会被忽略",
        "isCorrect": false,
        "description": "规则生效与否和 name 无关，name 仅作标识"
      }
    ]
  }
]
